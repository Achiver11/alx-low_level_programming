#include "main.h"
/**
 * _isupper - functipn yhat verifies if a character is uppercase or not
 * @c : tested character
 * Return : 1 if uppercase, 0 if not
 */
int _isupper(int c);
{
	if ((c >= 'A') && (c <= 'Z'))
	Return (0);
}
